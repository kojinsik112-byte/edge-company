# 엣지컴퍼니 앱 — PT 전 교체 체크리스트

발표(PT) 전에 아래 placeholder들을 **실제 정보**로 바꿔주세요.
모두 `index.html` 한 파일 안에 있습니다.

## 🔴 필수 (꼭 바꿔야 함)

| 위치 | 현재 값 | 바꿀 내용 |
|---|---|---|
| `index.html` 연락처 설정부 (상단 주석 표시) | `EDGE_PHONE = '052-XXX-XXXX'` | **실제 대표 전화번호** — 이 한 곳만 고치면 전화상담 표시·전화걸기 둘 다 자동 반영됨 |
| 같은 위치 | `EDGE_KAKAO_URL = 'https://pf.kakao.com/_xnLLxnL'` | **실제 카카오 채널 주소** |
| 협력사 프로필 입력칸 | `value="055-XXX-XXXX"` | 협력사 데모용 번호 (시연용이면 그대로 둬도 됨) |
| 유튜브 카드 링크 | `youtube.com/channel/UCYxheq7nt_7Pz4dBNd_zx1Q...` | 엣지컴퍼니 실제 유튜브 채널인지 확인 |

## 🟡 더미 데이터 (시연용 — 그대로 둬도 무방)

- 후기/견적 연락처 `010-1234-5678`, `010-2345-6789` 등 → 가짜 번호
- 인증번호 `1234 / 5678` → 목업 (실제 SMS 발송 아님)
- 입주민/협력사 이름, 동·호수, 후기 내용 → 전부 예시 데이터

## ⚠️ 시연 시 주의

- "진짜 인증번호가 발송됩니다" 문구가 있지만 **실제 발송 기능은 없는 목업**입니다.
  시연 중 "인증번호 왜 안 와요?" 질문이 나올 수 있으니 미리 안내하세요.

## 🔒 나중에 실제 서비스(백엔드 연동) 전환 시 보안 작업

지금은 모든 데이터가 하드코딩이라 안전하지만, 입주민이 직접 입력한
후기·답글·견적 내용을 화면에 표시하게 되면 아래를 **반드시** 처리해야 합니다.

- `innerHTML`로 사용자 입력을 출력하는 부분 → `textContent` 또는 이스케이프 처리
  (현재 후기 답글, 후기 목록 렌더링 등 여러 곳)
- 이걸 안 하면 악성 후기로 **XSS 공격**이 가능해집니다.
